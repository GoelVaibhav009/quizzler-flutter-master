class Question {
  String questionText;
  bool questionAnswer;

  Question({String q, bool a}) {
    questionAnswer = a;
    questionText = q;
  }
  // Constructor
  // Question(this.questionText, this.questionAnswer) {};
}
